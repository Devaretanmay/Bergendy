[package]
name = "boundary-core"
version = "1.1.3"
edition = "2021"
license = "Apache-2.0"
description = "Boundary - autonomous software maintenance for systems that change (Rust core)"
repository = "https://github.com/Devaretanmay/Boundary"
homepage = "https://github.com/Devaretanmay/Boundary"
documentation = "https://docs.rs/crate/boundary-core"
keywords = ["ai", "mcp", "execution", "sandbox"]
categories = ["development-tools", "command-line-utilities"]

[lib]
name = "boundary_core"
crate-type = ["cdylib", "lib"]

[features]
default = []
# Python extension module (enabled by maturin). Plain `cargo build`
# without this feature produces a clean FFI cdylib for the TS SDK.
pyo3-binding = ["dep:pyo3", "pyo3/extension-module", "pyo3/abi3-py38"]

[dependencies]
pyo3 = { version = "0.29", features = ["abi3-py38"], optional = true }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = { version = "1.0.150" }
regex = "1"
blake3 = "1"

[target.'cfg(target_os = "linux")'.dependencies]
landlock = "0.4"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = "symbols"
